---
description: CrewAI 电商营销系统的项目专用开发规范，涵盖技术栈、目录结构、代码风格、接口返回与提示词约束
alwaysApply: true
---

# 项目专用开发规范

## 1. 技术栈选型原则
- 优先采用实用、成熟、易维护的技术栈，避免过度复杂化
- 前端推荐：`Next.js + TypeScript + Hero UI + Tailwind CSS`
- 状态管理按复杂度选择，优先轻量方案，避免无必要的全局状态膨胀
- 后端优先选择团队最熟悉、可快速交付的方案，接口设计必须稳定、清晰、可扩展
- 如果技术方案存在多种可行选择，优先选“开发效率高、维护成本低、联调友好”的方案

## 2. 前端风格与 UI 要求
- 页面要“炫酷但克制”，强调高级感、层次感、留白和信息密度平衡
- 必须兼顾实用性，不做无法落地的重动画、重特效、重交互设计
- 优先使用 `Hero UI` 作为基础组件体系，统一按钮、表单、弹窗、表格、标签、卡片等视觉语言
- 视觉设计要围绕电商营销内容场景，突出内容生成、对比、预览、导出、联调状态展示
- 页面必须支持清晰的加载态、空状态、错误态、成功态

## 3. 目录结构约定
- 目录结构必须清晰分层，避免页面、业务、组件、API、类型文件混放
- 推荐拆分为：
  - `app/` 或 `pages/`：页面与路由
  - `components/`：通用组件
  - `features/`：业务模块
  - `services/` 或 `api/`：接口请求
  - `types/`：类型定义
  - `utils/`：通用工具
  - `constants/`：常量配置
- 与 CrewAI、文案生成、SEO、社媒输出相关的模块必须保持职责单一

## 4. 代码风格要求
- 使用 TypeScript 严格类型，避免 `any`，确有必要时必须说明原因
- 函数、变量、组件命名要语义明确，保持一致的命名风格
- 组件要尽量保持可复用、可测试、职责单一
- 优先使用函数式写法和组合式结构，避免过度嵌套
- 所有异步请求必须处理加载、失败、空数据三种状态
- 新增逻辑必须考虑可扩展性，但不要提前设计过度

## 5. 接口返回与数据结构要求
- 所有接口必须遵循统一返回结构：
  - `code`
  - `message`
  - `data`
- 前后端字段命名必须统一，禁止同一字段多种写法并存
- 返回数据必须可直接映射到前端展示，不允许依赖前端猜测字段含义
- 列表接口必须包含分页信息、总数信息和当前页信息
- 时间字段统一使用 ISO 8601 或明确约定的时间戳格式，不能混用

## 6. 提示词与 AI 生成约束
- 提示词必须结构化、清晰、可复用，避免冗长堆砌
- 生成营销内容时要强调真实、可信、可转化，禁止虚假夸大
- 如果信息不足，先输出“待确认”字段和合理假设，再继续生成
- 智能体输出必须统一口径，禁止不同模块给出冲突结论
- 输出结果应同时兼顾：商品页、详情页、海报、图片创意、视频脚本、视频素材、SEO、社媒传播、接口联调

## 7. 交付标准
- 页面效果要有产品感和完成度，但必须控制在可实现范围内
- 所有功能都要支持前后端联调，不允许只做展示不做数据协议
- 关键模块需要提供示例数据、接口示例和字段说明
- 任何新增功能都要优先保证稳定、可维护、可迭代
